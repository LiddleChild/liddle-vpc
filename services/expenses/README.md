# Expenses

Mobile-first Next.js PWA for viewing outstanding monthly budget data.

## Commands

```bash
pnpm install
pnpm dev
pnpm run lint
pnpm run build
```

The app uses the Next.js App Router, TypeScript, Tailwind CSS, and shadcn/ui.
